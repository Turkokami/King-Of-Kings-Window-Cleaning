# The gate scripts (word-count, dedup) parse frontmatter on LF line endings.
# With core.autocrlf on Windows, a checkout turned content to CRLF and the
# substance gate stopped finding localFacts. Keep text LF on every platform.
* text=auto eol=lf
*.webp binary
*.png binary
*.jpg binary
*.woff2 binary
